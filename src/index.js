import React from 'react';
import ReactDOM from 'react-dom/client';
import { NotasApp } from './NotasApp';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<NotasApp />);
